apply plugin: "war"

bootWar {
    mainClass = "com.soa.employee.EmployeeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
